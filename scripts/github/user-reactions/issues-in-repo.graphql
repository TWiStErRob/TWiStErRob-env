query {
    repository(owner: "cli", name: "cli") {
        __typename
        url
        issues(
            first: 100,
            orderBy: { field: CREATED_AT, direction: DESC },
        ) {
            nodes {
                ...issueDetails
            }
        }
    }
}
