query {
    repository(owner: "community", name: "community") {
        discussions(first: 100) {
            nodes {
                isAnswered
                ...issueDetails
            }
        }
    }
}
